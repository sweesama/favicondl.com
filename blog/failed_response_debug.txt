```json
{
  "content": "<h1>Nuxt 应用 Head Favicon：掌握 useHead 进行图标控制</h1>\n\n<p>你刚把 Nuxt 应用部署上线,浏览器标签页里却还是默认 favicon。更糟的是,市场团队要求结账页和管理后台用不同的图标。在模板里硬编码 link 标签根本不够用 —— Nuxt 通过 <code>app.head</code> 配置和 <code>useHead</code> 组合式函数提供了程序化的控制能力,把这两者搞清楚,决定了你是在跟框架较劲,还是让框架为你所用。</p>\n<p>本文聚焦于通过 Nuxt 的 head 配置系统管理 favicon,而不是我们 <a href='/blog/nuxt-favicon.html'>Nuxt favicon 指南</a> 中介绍的文件式元数据方案。如果你需要按页面切换图标、深色模式自动切换,或运行时动态替换,head API 才是正解。</p>\n<h2>前置条件</h2>\n<ul>\n<li>Nuxt 3.x 或 4.x 项目</li>\n<li>已生成 favicon 文件(PNG、SVG、ICO)—— 不确定需要哪些规格的话,可以参考 <a href='/blog/favicon-sizes-guide.html'>favicon 尺寸指南</a></li>\n<li>对 Vue 3 <code>&amp;lt;script setup&amp;gt;</code> 语法有基本了解</li>\n</ul>\n<h2>第一步:通过 app.head 设置默认 Favicon</h2>\n<p><code>nuxt.config.ts</code> 中的 <code>app.head</code> 属性是整个框架级别的默认值。这里写的内容会被注入到每个页面的 <code>&amp;lt;head&amp;gt;</code> 里,除非被具体页面覆盖。</p>\n<pre><code>// nuxt.config.ts\nexport default defineNuxtConfig({\n  app: {\n    head: {\n      link: [\n        { rel: 'icon', type: 'image/svg+xml', href: '/favicon.svg' },\n        { rel: 'icon', type: 'image/png', sizes: '32x32', href: '/favicon-32x32.png' },\n        { rel: 'icon', type: 'image/x-icon', href: '/favicon.ico' },\n        { rel: 'apple-touch-icon', sizes: '180x180', href: '/apple-touch-icon.png' }\n      ]\n    }\n  }\n})</code></pre>\n<p>把图标文件放到 <code>public/</code> 目录下,Nuxt 就会从根路径提供这些资源。根据 <a href='https://nuxt.com/docs/4.x/getting-started/seo-meta'>Nuxt SEO 与 meta 文档</a>,<code>link</code> 数组的格式与底层 <code>unhead</code> 引擎一致,而 unhead 又遵循 WHATWG 在 <a href='https://html.spec.whatwg.org/multipage/links.html#rel-icon'>html.spec.whatwg.org</a> 上定义的 <code>rel="icon"</code> 规范。</p>\n<h2>第二步:用 useHead 在单页内覆盖 Favicon</h2>\n<p>有时候你希望某些路由用别的图标。<code>useHead</code> 组合式函数可以让你写入仅对该页面生效、覆盖全局配置的条目。</p>\n<pre><code>&amp;lt;script setup lang=\"ts\"&amp;gt;\nuseHead({\n  link: [\n    { rel: 'icon', type: 'image/svg+xml', href: '/icons/admin-favicon.svg' }\n  ]\n})\n&amp;lt;/script&amp;gt;\n\n&amp;lt;template&amp;gt;\n  &amp;lt;div&amp;gt;Admin Dashboard&amp;lt;/div&amp;gt;\n&amp;lt;/template&amp;gt;</code></pre>\n<p>把 <code>useHead()</code> 调到 <code>&amp;lt;script setup&amp;gt;</code> 块顶部。Nuxt 会处理 SSR 注入,覆盖项会出现在初始 HTML 里 —— 不会出现 hydration 时图标闪一下的问题,也不需要客户端打补丁。</p>\n<h2>第三步:根据状态动态切换 Favicon</h2>\n<p>想让 favicon 跟着通知数量或主题变化吗?把 <code>useHead</code> 和响应式的 <code>ref</code> 或 <code>computed</code> 配合起来就行。</p>\n<pre><code>&amp;lt;script setup lang=\"ts\"&amp;gt;\nconst cartCount = useCartCount()\n\nuseHead({\n  link: [\n    {\n      rel: 'icon',\n      type: 'image/svg+xml',\n      href: computed(() =&amp;gt; \n        cartCount.value > 0 \n          ? `/icons/cart-${cartCount.value}.svg` \n          : '/favicon.svg'\n      )\n    }\n  ]\n})\n&amp;lt;/script&amp;gt;</code></pre>\n<p>只要 <code>cartCount</code> 变化,computed 出的 href 就会更新。Nuxt 的 <code>useHead</code> 会追踪响应式依赖并原地改写 <code>&amp;lt;link&amp;gt;</code> 标签 —— 不会整页刷新,也不需要手动操作 DOM。如果你想用 canvas 绘制角标继续深入,这套底层模式在我们 <a href='/blog/dynamic-favicon-javascript.html'>JavaScript 动态 favicon 指南</a> 里也有覆盖。</p>\n<h2>第四步:支持深色模式 Favicon</h2>\n<p>现代浏览器会识别 icon link 上的 <code>media</code> 属性。配合 <code>useHead</code>,无需任何 JavaScript 就能实现随主题切换的图标。</p>\n<pre><code>useHead({\n  link: [\n    { rel: 'icon', type: 'image/svg+xml', href: '/favicon-light.svg', media: '(prefers-color-scheme: light)' },\n    { rel: 'icon', type: 'image/svg+xml', href: '/favicon-dark.svg', media: '(prefers-color-scheme: dark)' }\n  ]\n})</code></pre>\n<p>浏览器会根据用户的系统偏好自动选择对应的那一个。这跟我们 <a href='/blog/favicon-dark-mode.html'>favicon 深色模式指南</a> 里介绍的是同一种思路,只是改用 Nuxt 的 head API 来驱动,而不是手写 HTML。</p>\n<h2>常见坑</h2>\n<ul>\n<li><strong>把 <code>useHead</code> 放到嵌套很深的子组件里。</strong> 也能跑,但 SSR 时机变得不可控,还可能出现重复标签。建议保持在页面级的 <code>&amp;lt;script setup&amp;gt;</code> 里。</li>\n<li><strong>忘了写 <code>type</code> 属性。</strong> 当多个 <code>rel=\"icon\"</code> link 路径或文件名相同时,浏览器可能给出错误的文件。为了清晰,务必设置 <code>type</code>。</li>\n<li><strong>把文件式元数据和 <code>useHead</code> 混着用。</strong> Nuxt 的文件式图标(放在 <code>app/</code> 或 <code>public/</code>)可能跟显式声明的 <code>app.head</code> 条目冲突,导致出现重复的 <code>&amp;lt;link&amp;gt;</code> 标签。一个项目里只选一种方案。</li>\n<li><strong>硬编码绝对路径。</strong> 用根相对路径,例如 <code>/favicon.svg</code>,这样跨路由、子域名、反向代理后面都能正确加载。</li>\n</ul>\n<h2>收尾</h2>\n<p><code>app.head</code> 配置负责默认值,<code>useHead</code> 负责所有动态场景。两者配合,Nuxt 应用里常见的 favicon 需求基本都能覆盖 —— 从单个静态图标,到按页面切换、再到响应式角标 —— 不用额外装第三方 head 插件,也不用和 Nuxt 的默认行为较劲。</p>\n<p>如果你还没生成图标文件,<a href='/index.html'>Mzu favicondl</a> 一键就能输出完整的多分辨率包(SVG、PNG、ICO、Apple Touch)。把文件丢进 <code>public/</code>,在 <code>app.head</code> 里接好,几分钟之内你的标签页就能看起来专业多了。</p>"
}
```